{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Lua",
            "type": "lua",
            "request": "launch",
            "program": "${file}",
            "cwd": "${fileDirname}"
        }
    ]
}